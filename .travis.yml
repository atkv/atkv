language: c
sudo: required
dist: trusty
compiler: gcc
script:
  - mkdir build
  - cd build
  - cmake -DBUILD_TESTING=ON -DCMAKE_BUILD_TYPE=Debug -DBUILD_STUDIO=OFF -DCOVERALLS=ON ..
  - make
after_success:
  - make coveralls


